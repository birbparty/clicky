# Package
version       = "0.1.0"
author        = "Birb Party"
description   = "Minimal idle clicker — treeform (windy + boxy) edition."
license       = "MIT"
srcDir        = "src"
bin           = @["clicky"]

# Dependencies
requires "nim >= 2.0.0"
requires "windy >= 0.1.0"
requires "boxy >= 0.4.0"
requires "opengl >= 1.1.0"
